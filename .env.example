# Application Settings
APP_NAME=Secure Python App
ENV=production
HOST=127.0.0.1
PORT=5000

# Docker Hub Credentials (set in GitHub Secrets, never commit real values)
# DOCKER_USERNAME=your_dockerhub_username
# DOCKER_PASSWORD=your_dockerhub_token
